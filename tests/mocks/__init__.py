"""Mock services committed for integration testing.

Each subpackage is a runnable mock of an external dependency the registry
talks to. Mocks are test-only and never imported by production code.
"""
